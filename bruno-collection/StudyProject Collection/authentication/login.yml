info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {"email":"test5bcrypt@example.com",
      "password":"123456"}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
